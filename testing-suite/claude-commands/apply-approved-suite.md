---
description: Run the testing-suite approved-fix implementation and validation flow
argument-hint: "[approved findings or scope]"
---

Read and run `skill://testing-suite/playbooks/apply-approved-suite.md`.

Use `$ARGUMENTS` as the approved findings or target scope if provided.

Only `TechLead` may write; run `Validator` after implementation.
